[project]
name = "mbtiles-vietnam-backend"
version = "0.1.0"
description = "FastAPI backend for MBTiles Vietnam"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "shapely",
    "httpx"
]